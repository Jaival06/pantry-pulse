# Optional live AI provider configuration. Keep real keys in .env only.
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b

# OpenAI remains supported as a fallback provider.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
